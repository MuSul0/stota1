// Ändern Sie diesen Import:
import { SessionProvider } from '@/components/SessionProvider';
// Zu:
import SessionProvider from '@/components/SessionProvider';
// Oder behalten Sie den named import bei, aber stellen Sie sicher, dass er mit den exports übereinstimmt